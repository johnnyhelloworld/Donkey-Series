{% extends 'base.html.twig' %}

{% block title %}Season #{{ season.id }}{% endblock %}

{% block body %}
<div class="container">
    <h1 class="my-5 text-center">Season #{{ season.id }}</h1>
    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ season.number }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ season.year }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ season.description }}</td>
            </tr>
        </tbody>
    </table>
    <div class="btn-group">
        <a class="btn btn-primary" href="{{ path('season_edit', {'id': season.id}) }}">Edit</a>
        {{ include('season/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}
