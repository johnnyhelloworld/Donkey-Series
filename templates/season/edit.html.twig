{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1>Éditer une saison</h1>
    <div class="container">
        {{ include('season/_form.html.twig', {'button_label': 'Éditer une saison'}) }}
        {{ include('season/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
