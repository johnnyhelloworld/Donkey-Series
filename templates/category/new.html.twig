{% extends "base.html.twig" %}

{% block body %}
<div class="container my-3">
    <h1 class="text-center">Create a new category</h1>
    <div class="row">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary" type="submit" formnovalidate>Add category</button>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}